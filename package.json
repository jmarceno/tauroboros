{
  "name": "tauroboros",
  "version": "0.1.0",
  "description": "TaurOboros - AI-powered workflow orchestration system with container isolation",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "predev": "bun run scripts/generate-version.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --target=bun src/index.ts --outdir dist && bun run kanban:build",
    "compile": "bun run scripts/compile.ts",
    "compile:test": "bun run scripts/test-binary.ts",
    "kanban:build": "cd src/kanban-vue && npm install && npm run build",
    "kanban:dev": "SERVER_PORT=3789 bun run src/index.ts & sleep 3 && cd src/kanban-vue && npm install && SERVER_PORT=3789 npm run dev",
    "test": "bun test tests/*.test.ts",
    "test:e2e": "bun run tests/e2e/prepare.ts && bunx playwright test",
    "test:e2e:ui": "bun run tests/e2e/prepare.ts && bunx playwright test --ui",
    "test:e2e:debug": "bun run tests/e2e/prepare.ts && bunx playwright test --debug",
    "test:e2e:headed": "bun run tests/e2e/prepare.ts && bunx playwright test --headed",
    "test:e2e:real": "bun run tests/e2e/prepare.ts container && TEST_TYPE=real-workflow bunx playwright test tests/e2e/real-workflow.spec.ts",
    "skills:sync": "bun run scripts/sync-skills.ts",
    "skills:install": "bun run scripts/sync-skills.ts --clean",
    "skills:verify": "bun run scripts/verify-setup.ts",
    "setup": "bun run skills:install && bun run skills:verify",
    "container:setup": "./scripts/setup-e2e-tests.sh",
    "container:verify": "bun run scripts/verify-podman.ts",
    "container:cleanup": "./scripts/cleanup-gvisor.sh",
    "container:build": "podman build -t pi-agent:alpine -f docker/pi-agent/Dockerfile ."
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "@types/bun": "latest",
    "bun-types": "^1.3.11"
  },
  "dependencies": {
    "@tiptap/extension-code-block": "^3.22.3",
    "@tiptap/extension-link": "^3.22.3",
    "@tiptap/extension-placeholder": "^3.22.3",
    "@tiptap/extension-underline": "^3.22.3",
    "@tiptap/starter-kit": "^3.22.3",
    "@tiptap/vue-3": "^3.22.3"
  }
}
